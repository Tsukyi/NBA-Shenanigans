name: NBA Shenanigans checks
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Simulation and roster tests
        run: npm test
      - name: Build static site
        run: npm run build
      - name: Install browser tools
        run: |
          npm install --no-save --package-lock=false playwright@1.62.1
          npx playwright install --with-deps chromium
      - name: Test complete browser journeys
        run: |
          npm start > /tmp/nba-server.log 2>&1 &
          for i in $(seq 1 30); do curl -fsS http://localhost:5174/ > /dev/null && break; sleep 1; done
          npm run test:e2e
      - name: Save browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nba-browser-screenshots
          path: artifacts/
          if-no-files-found: ignore
